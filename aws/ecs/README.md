# Usage

Initialize terraform configuration files
```bash
$ terraform init
```

Create terraform variables file
```
$ vi terraform.tfvars
```

Provisioning aws resources
```bash
$ terraform plan
$ terraform apply
```